@model MedicalHistory

<style>
                body {font-family: "Times New Roman", Georgia, Serif;}
                h1, h2, h3, h4, h5, h6 {
                font-family: "Playfair Display";
                letter-spacing: 5px;
                }
</style>

<partial name="_BreadCrumbs" model=@(new [] {
    ("/","Home"),
    ("/dog","Dogs"),
    ($"/dog/details/{Model.Id}",$"{Model.Id}"),
    ("","Delete MedicalHistory")
}) /> 

<h3 class="mt-4 mb-4">Delete Medical History Note</h3>

<h4 class="mb-4">Are you sure you want to delete this Medical History Note?</h4>

<div>
    <div class="col card rounded shadow p-3">
        <dl class="row">       
            <dt class="col-5">Created On</dt>
            <dd class="col-7">@Model.CreatedOn.ToShortDateString()</dd>

            <dt class="col-5">Report</dt>
            <dd class="col-7">@Model.Report</dd>
                     
        </dl>
    </div>

    <div class="mt-4">
        <form asp-action="MedNoteDeleteConfirm" type="post">
            <input type="hidden" asp-for="Id"/>
            <input type="hidden" asp-for="DogId"/>
            
            <input type="submit" value="Confirm" class="btn btn-danger" />
            <a class="btn btn-secondary" asp-controller="Dog" asp-action="Details" asp-route-id="@Model.Dog.Id" >Cancel</a>
        </form>
    </div>
    
</div>